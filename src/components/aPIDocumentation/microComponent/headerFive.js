'use client'
const HeaderFive = ({children, className}) => {
    return (
        <h3 className={`anek-latin-font text-[14px] lg:text-base font-bold ${className}`}>{children}</h3>
    );
};

export default HeaderFive;